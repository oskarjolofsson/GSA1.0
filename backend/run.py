import uvicorn

# Use this file to run the fastapi application with `python run.py`

if __name__ == "__main__":
    uvicorn.run(
        "app.main:app",
        host="0.0.0.0",
        port=8000,
        reload=True,
    )