// Shared motion tokens for the intro's step transitions. Mirrors
// features/library/animations.ts.

export const INTRO_ANIM = {
    /** Step transition duration (ms). */
    stepDuration: 150,
} as const;
