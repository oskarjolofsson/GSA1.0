import { View, Text } from "react-native";

export default function AnalysisScreen() {
  return (
    <View style={{ flex: 1, alignItems: "center", justifyContent: "center" }}>
      <Text>Analysis (coming later)</Text>
    </View>
  );
}
